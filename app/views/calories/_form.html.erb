<%= form_with(model: calory) do |form| %>
  <% if calory.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(calory.errors.count, "error") %> prohibited this calory from being saved:</h2>

      <ul>
        <% calory.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :calories_number, style: "display: block" %>
    <%= form.number_field :calories_number, required: true %>
  </div>

  <div>
    <%= form.label :is_burned, style: "display: block" %>
    <%= form.check_box :is_burned %>
  </div>

  <div>
    <%= form.label :doing_what, style: "display: block" %>
    <%= form.text_area :doing_what, required: true %>
  </div>

  <div>
    <%= form.submit class:"btn btn-primary" %>
  </div>
<% end %>
